{
  "name": "til-blog",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/gen-security-txt.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "vitest run",
    "check": "astro check && biome check . && vitest run"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.7.3",
    "@sentry/browser": "^10.67.0",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@biomejs/biome": "^2.5.1",
    "sharp": "^0.35.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
